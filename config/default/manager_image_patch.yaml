apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        image: ghcr.io/joshuagrisham/cluster-api-provider-libvirt:0.1
        imagePullPolicy: Always
